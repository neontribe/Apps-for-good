<?php

function block_rewrite_preprocess_block(&$vars, $hook) {
  if ($hook == 'block') {
    if(strlen($vars['block']->subject) > 0) {
		$vars['block']->subject = '<h2>' . $vars['block']->subject . '</h2>';
	}
  }
}

?>